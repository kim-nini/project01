<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezen.grrreung.domain.member.mapper.MemberMapper">

    <insert id="create" parameterType="Member">
        INSERT INTO member(member_id, password, name, hp, email, address)
        VALUES (#{memberId}, #{password}, #{name}, #{hp}, #{email}, #{address})
    </insert>

</mapper>
